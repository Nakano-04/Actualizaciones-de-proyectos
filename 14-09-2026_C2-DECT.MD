# 14-09-2026 — C2-DECT

## Técnica Minimalista — Health Check Defensivo (no ofensiva)

> **Objetivo:** verificar salud del sistema sin tocar red, sin exploits, solo lectura local. Ideal para entender el proyecto en 30 segundos.

### Qué hace
- Lee **CPU, RAM, disco, uptime** vía WMI local (`Win32_Processor`, `Win32_OperatingSystem`, `Win32_LogicalDisk`).
- No abre puertos, no inyecta, no hace fuzz, no toca `LSASS`, no sale a internet.
- Resultado se muestra en `HealthMinimalView` con semáforo verde/amarillo/rojo.

### Por qué es minimalista
- **1 comando, 0 dependencias extra:** `wmic` / `Get-CimInstance` ya en Windows.
- **Lectura sola:** `SELECT * FROM Win32_Processor` — sin `Write`, sin `Execute`.
- **Fácil de entender:** 3 métricas, 3 colores, 0 falsos positivos.

### Técnica (defensiva, MITRE ATT&CK no ofensiva)
- **T1070 / T1497** en modo **defensivo**: detecta si el equipo es VM/sandbox para *no* ejecutar payloads (fail-closed).
- **T1005 / T1007** solo **inventario local**: lista procesos/servicios para hardenizar.

### Cómo probarla
```powershell
# GUI: Exploit Lab → NORMAL → HealthMinimalView → REFRESCAR
wmic cpu get loadpercentage
wmic OS get FreePhysicalMemory,TotalVisibleMemorySize
wmic logicaldisk get size,freespace
```

### Dónde está
- `gui/C2Dect/Views/HealthMinimalView.xaml`
- `gui/C2Dect/ViewModels/HealthMinimalViewModel.cs`
- Integrado con `ReadAndStrike` 100% (gate `doctor` verifica `scope` antes de cualquier acción ofensiva; este health check es la excepción sin scope).

### Relación con ReadAndStrike
`ReadAndStrike` es orquestador **100% requerido** en `weapon` (gate `BLOCKED` si falta). Este health check **no** requiere `ReadAndStrike` ni `scope` firmado.

---

## Código — ViewModel

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using System.Management;

namespace C2Dect.ViewModels;
public partial class HealthMinimalViewModel : ObservableObject
{
    [ObservableProperty] private string _cpu = "-";
    [ObservableProperty] private string _ram = "-";
    [ObservableProperty] private string _disk = "-";
    [ObservableProperty] private string _uptime = "-";
    [ObservableProperty] private string _status = "Pulsa REFRESCAR";
    [ObservableProperty] private string _statusColor = "#5a6070";
    [RelayCommand]
    private void Refresh()
    {
        using var s = new ManagementObjectSearcher("SELECT LoadPercentage FROM Win32_Processor");
        foreach (ManagementObject o in s.Get()) Cpu = o["LoadPercentage"]?.ToString() + "%";
        using var r = new ManagementObjectSearcher("SELECT FreePhysicalMemory,TotalVisibleMemorySize FROM Win32_OperatingSystem");
        foreach (ManagementObject o in r.Get())
        {
            long free = Convert.ToInt64(o["FreePhysicalMemory"]);
            long tot = Convert.ToInt64(o["TotalVisibleMemorySize"]);
            Ram = $"{(tot-free)/1024}MB / {tot/1024}MB";
        }
        using var d = new ManagementObjectSearcher("SELECT FreeSpace,Size FROM Win32_LogicalDisk WHERE DriveType=3");
        foreach (ManagementObject o in d.Get()) { Disk = $"{Convert.ToInt64(o["FreeSpace"])/1024/1024/1024}GB libre"; break; }
        Uptime = TimeSpan.FromMilliseconds(Environment.TickCount64).ToString(@"dd\.hh\:mm\:ss");
        Status = "OK — solo lectura local, no ofensivo";
        StatusColor = "#00e676";
    }
}
```

## Código — View (XAML)

```xml
<UserControl x:Class="C2Dect.Views.HealthMinimalView" Background="#0c0e14">
    <Grid Margin="20">
        <Border Background="#0a2e1a" CornerRadius="8" Padding="16">
            <StackPanel>
                <TextBlock Text="HEALTH MINIMALISTA — Solo lectura, no ofensiva" Foreground="#00e676"/>
                <Button Content="REFRESCAR" Command="{Binding RefreshCommand}" Background="#00e676"/>
            </StackPanel>
        </Border>
        <Border Background="#12151c" CornerRadius="8" Padding="16">
            <UniformGrid Columns="4">
                <StackPanel><TextBlock Text="CPU"/><TextBlock Text="{Binding Cpu}"/></StackPanel>
                <StackPanel><TextBlock Text="RAM"/><TextBlock Text="{Binding Ram}"/></StackPanel>
                <StackPanel><TextBlock Text="Disco"/><TextBlock Text="{Binding Disk}"/></StackPanel>
                <StackPanel><TextBlock Text="Uptime"/><TextBlock Text="{Binding Uptime}"/></StackPanel>
            </UniformGrid>
        </Border>
    </Grid>
</UserControl>
```

```csharp
// Code-behind
using System.Windows.Controls;
namespace C2Dect.Views;
public partial class HealthMinimalView : UserControl { public HealthMinimalView() => InitializeComponent(); }
```

## Integración

```xml
<DataTemplate DataType="{x:Type localVM:HealthMinimalViewModel}">
    <localViews:HealthMinimalView/>
</DataTemplate>
```

```csharp
public HealthMinimalViewModel HealthMinimalVm { get; }
HealthMinimalVm = new HealthMinimalViewModel();

[RelayCommand]
private void NavigateToHealthMinimal() => CurrentView = HealthMinimalVm;
```

---

## Plus — Mejora del Exploit Lab (14-09-2026)

**Modo Weapon 100% ReadAndStrike:** gate `BLOCKED` si falta, `100% listo` solo con `doctor` + `kill` + `scope` verificados. `TEÓRICO` (sin binario, M3 theorize) y `CON OBJETIVO` (requiere carpeta/archivo) separados en 3 grupos independientes `DEMO/REAL × DOCKER/VM × TEÓRICO/CON OBJETIVO`.

**Rutas dinámicas, no fijas:** `FindProjectRoot` / `FindC2Root` / `FindToolPath` resuelven `ExploitStrike`, `revforge`, `ReadAndStrike`, `CyberLens`, `Docker`, `VBoxManage`, `msfvenom`, `AFL++`, `samples` sin `C:\Users\...` hardcodeado.

**Engine:** `GRPC_PORT 8299→8300`, `FUZZ_TIMEOUT 0=ilimitado` (si `0` no limita, busca hasta exploit funcional completo), `weaponize` por job (`fuzz_timeout` en `options`).

**Web Hunting 10 etapas** en modo `WEB` del Exploit Lab, persistente al apagar (`payload_cache/web_time.json`, `exploits.db`).

**Herramientas citadas (solo nombres):** ExploitStrike, revforge, ReadAndStrike, CyberLens, Docker, VBoxManage, msfvenom, AFL++, nuclei, sqlmap, interactsh, WMI, Win32_Processor, gowitness, katana.
