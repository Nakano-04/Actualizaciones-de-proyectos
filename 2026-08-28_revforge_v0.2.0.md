# 2026-08-28 revforge v0.2.0

## Características públicas

### Nuevo tool zero_day_intel (12 tools)
- **Agregación INTEL**: Endpoint único `POST /api/zero-day-intel` que combina `fortify` (mitigaciones ASLR/NX/Canary/CFG), `vuln` (funciones peligrosas `strcpy`/`sprintf`/`format` con `call_sites`), `rop` (50 gadgets `pop rcx; ret`), `harness` (wrapper C/Python con `LLVMFuzzerTestOneInput`), `capa` (técnicas MITRE) y `feasibility` en una sola llamada
- **Secciones .text/.rdata/UPX**: Escaneo de secciones ELF/PE con `size` y `entropy`, marcado `is_text` y `is_packed` (UPX0/UPX1) para detectar binarios empaquetados
- **Feasibility 0-100**: Cálculo `100 - fortify + vuln*6 + gadgets/20` con `no_target < 70` → `NO_TARGET` archivado en `CyberLens` sin gastar `FUZZ`, `>=70` → `proceder a FUZZ`
- **Mitigations dict**: Retorna `{"aslr": true, "dep": false, "cfg": true, ...}` para 15 checks PE/ELF con severidad `alta/media/baja`

### ROP live con fingerprint SMB
- **Fingerprint SMB**: `SMB_COM_NEGOTIATE` parsea `NativeOS` y `BuildNumber` (7601 vs 7600) sin credenciales
- **Validación dinámica**: `find_gadgets_live` con `pefile` + `capstone` y `validate_rop` que verifica `base ≤ addr < base+size` con fallback a `default_rop_pool` de `A1`
- **Capstone**: Soporte x86/x64/ARM64 `pop rcx/rdx/r8; ret` y `jmp rsp` con `CET` awareness
- **Tests 6**: `test_rop_live.py` con `find_gadgets_live`, `assemble_chain`, `validate` y `generate_live`

### Harness dirigido C/Python
- **Wrapper dirigido**: `harness_<target>.c` con `canary 0xDEADBEEF`, `harness(data,len)` que llama `strcpy`/`sprintf` según `familia`, y `LLVMFuzzerTestOneInput` para `libFuzzer`; alternativa `harness_<target>.py` con `bytearray(256)` para `probe.py`
- **Corpus y dict**: `dict_<target>.txt` con 16 base (`A*300/1200/2000`, `%s/%n/%p`, `\x00` `AAAA`) + por familia `strcpy 3` `format 4` `heap 3` y por `arch` `x86`/`arm64`, total 19-22 líneas
- **Compilación**: `compile_harness` con `clang -fsanitize=address -g -O1` `Linux` o `cl -GS-` `Windows`

### API POST /api/zero-day-intel
- **REST**: `POST /api/zero-day-intel` `{"path": "samples/fake_upx.dll", "deep": false}` y `GET /api/zero-day-intel?path=...`, `POST /api/mcp/call` con `zero_day_intel` y `GET /health` `capabilities: 12`

### Tests 10
- `test_zero_day_intel.py` con `tool_registered`, `dispatch_basic`, `api_post`, `mcp_call`, `fase_a1_completa` (`secciones_analizadas`, `pool_size`, `mitigations`, `no_target`) y `no_target_low_feasibility` (52 `NO_TARGET` para `sample_elf`)
